# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU
    - NOTHING=NEW
    
build:
  pre_ci:
    
  pre_ci_boot:
   
  ci:
  - docker push davidgdragon/fakeimage
  
  post_ci:

hub:
  - integrationName: realdocker
    type: docker
