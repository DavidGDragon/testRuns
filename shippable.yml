# Language setting
language: node_js

# Version number
node_js:
  - 0.12

archive: true

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU
    - NOTHING=NEW

build:
  pre_ci:
    
  pre_ci_boot:
   - pip install pytest
    
  ci:
  - py.test --verbose

  post_ci:

  smoke_test:
    - sudo ls /

deploy:
  pre_build:
    - ls
    - ls -al
