# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU
    - NOTHING=NEW

matrix:
  include:
    - node_js: 3.6
      env: MAYO=BAD

build:
  pre_ci:
    
  pre_ci_boot:
   
  ci:
    - curl google.com:80
    
  post_ci:
