# Language setting
language: C

# Version number
node_js:
  - 1

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU
    - NOTHING=NEW

build:
  pre_ci:
    
  pre_ci_boot:
   
  ci:
    - curl google.com:80
    
  post_ci:
