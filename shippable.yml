# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU
    - NOTHING=NEW
    
build:
  pre_ci:
    docker build -t=davidgdragon/fakeimage .
  pre_ci_boot:
   
  ci:

  post_ci:
  - docker push davidgdragon/fakeimage

hub:
  - integrationName: realdocker
    type: docker
    branches:
        only:
          - master
